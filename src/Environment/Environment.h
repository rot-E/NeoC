#pragma once

/* ------------------------------------------------------ */
/* 環境開始宣言 */
#define $_ { _S._Setup(); E.Try(({ void Try() {

/* 環境終了宣言 */
#define _$ }; Try; }), ({ void Catch() { _S._HANDLER(); }; Catch; }), ({ void Finally() {}; Finally; })); }
/* ------------------------------------------------------ */
